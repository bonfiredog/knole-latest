///Set Maximum Of All Internal States To 100 & Minimum To 0

Alive = clamp(Alive,0,100)
Tiredness = clamp(Tiredness,0,100)
Pain = clamp(Pain,0,100)
Thirst = clamp(Thirst,0,100)
Temperature = clamp(Temperature,0,100)
Lust = clamp(Lust,0,100)
Stress = clamp(Stress,0,100)
